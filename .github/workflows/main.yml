name: Main
on:
  schedule:
    - cron: '10 1,4,7,10,11,13,14,16,17,19,20,22,23 * * *'
jobs:
  execute:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Use Node.js 12
        uses: actions/setup-node@v1
        with:
          node-version: 12
      - name: install
        run: |
          yarn install
      - name: run
        run: |
          yarn start
        env:
          BASE_URL: ${{ secrets.BASE_URL }}
